[project]
name = "hyperframes-curated-intake-library"
version = "0.1.0"
description = "Source-aware HyperFrames curation skill and shared production library"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.23,<5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
