# Keyboard Object

Registry ID：`keyboard-object`；候选身份：`EDU-OBJECT-02`。

完整五行键盘物件，键帽具有真实层级、投影和确定性按压状态。

| 参数 | 说明 |
| --- | --- |
| `keys` | 二维键位数组；默认完整紧凑键盘 |
| `activeKeys` | 要按下和点亮的键名 |
| `label` | 快捷键动作说明 |

命名段：`enter`、`emphasis`、`dim`、`exit`。
