# Hero Big Type

Large CJK/English hero typography with balanced wrapping.

- `text`: required short phrase; explicit newlines are preserved.
- `durationFrames`: deterministic accent-rule reveal.
- `locale`: shared locale parameter controlling CJK/English geometry.

Legacy alias: `broll-hero.big-type`.
