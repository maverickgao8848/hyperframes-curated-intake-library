# Overlay Subtitle Highlight

Full-sentence subtitle with deterministic read/current/unread states.

- `text`: required full sentence.
- `keywordRanges`: up to six `{ start, end }` character ranges.
- `durationFrames`: local seek window.

Legacy alias: `aroll.subtitle-highlight`.
