# 仓库协作规则

## Skill 设计与修改

- 创建或修改 Skill 时，只增加实现目标所必需的关键约束。每条约束都应对应明确的风险、接口或验收要求；避免重复规则、过度限定和无实际作用的说明。
- 新增约束前，先检查其作用域内已有的 `AGENTS.md`、`SKILL.md`、引用文档、Schema、测试及其他约束源。已有权威定义时应引用或复用，避免复制出多个事实源。
- 若约束源之间存在歧义、重叠或潜在冲突，不得自行选择其一或悄然覆盖。先向用户说明冲突位置、影响范围和可选处理方式，商定后再修改。

## 系统性变更

- 修改前必须梳理相关上游输入、契约和生成流程，以及下游调用方、产物、验证与兼容性要求；判断局部修改对整个系统的影响后再实施。
- 修改应保持范围克制，但实现必须完整。需要联动的代码、文档、Schema、测试、调用点和迁移逻辑应同步更新，避免只修表面或留下半套新旧流程。
- 保持工作区干净：删除本次变更淘汰的死代码、重复实现、临时文件、无用产物和过时说明；非必要不保留兼容分支或备用副本，同时不得清理或覆盖用户无关的现有改动。
- 完成前执行与变更风险相称的验证，并检查差异，确认没有遗漏引用、残留垃圾或引入新的约束冲突。
