# Hero Inversion Flash

Finite black/white inversion beats that settle into the active skin.

- `text`: required short phrase.
- `flashes`: one to three finite cycles.
- `durationFrames`: total local window.

There is no timer or loop; `seek(localFrame)` directly resolves black, white, or settle. Legacy alias: `broll-hero.inversion-flash`.
