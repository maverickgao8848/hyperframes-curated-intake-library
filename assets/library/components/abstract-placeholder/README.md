# abstract-placeholder

Block export while a required asset or specification is missing.

- `data`: required project-provided semantic content.
- `title`: required teaching title.
- `sourceNote`: required source/date note.
- `durationFrames`: deterministic seek window.
- Readiness ceiling: `runtime-pass`; rendered DOM blocks export and requires replacement.

Must be replaced or explicitly approved before dispatch. Legacy alias: `broll-abstract.placeholder`.
