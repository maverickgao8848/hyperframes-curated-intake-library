# Hero Big Number

Hero-scale numeric evidence with mandatory provenance.

- `value`: required text value, preserving long-number formatting.
- `unit`: optional unit.
- `label`: required meaning.
- `sourceNote`: required source and date.
- `durationFrames`: deterministic duration.

Legacy alias: `broll-hero.big-number`.
