# Overlay Keyword Sticker

One to three controlled keyword stickers without autonomous timing.

- `items`: required `{ text, anchor, rotation }` list, maximum three.
- `durationFrames`: deterministic reveal window.

Rotation is constrained to −8°…8°. Legacy alias: `aroll.keyword-sticker`.
