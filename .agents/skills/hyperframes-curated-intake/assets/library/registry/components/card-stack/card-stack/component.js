import{createFidelityHost,destroyFidelity,htmlNode,standardTimelines}from"../fidelity-runtime.js";
const CSS=`
.comp-card-stack{width:100%;min-height:420px;display:grid;place-items:center;border-radius:24px;background:radial-gradient(circle,#252033,#090a0e 58%);color:#fff;font-family:var(--comp-font-sans,sans-serif);perspective:1000px}
.comp-card-stack .cst-stage{position:relative;width:520px;height:320px;transform-style:preserve-3d}.comp-card-stack .cst-card{position:absolute;inset:18px 45px;padding:28px;border:1px solid #ffffff35;border-radius:24px;background:linear-gradient(140deg,#343043,#151720);box-shadow:0 30px 65px #0008;transform-origin:50% 100%}.comp-card-stack .cst-card[data-active=true]{border-color:#aa88ff;background:linear-gradient(140deg,#463a68,#171821)}.comp-card-stack .cst-no{font:600 10px/1 var(--comp-font-mono,monospace);letter-spacing:.18em;color:#bda7ff}.comp-card-stack .cst-label{position:absolute;left:28px;bottom:28px;font-size:35px;font-weight:760}
`;
export const meta={id:"card-stack",version:2,candidateId:"EDU-DEPTH-02"};
export function mount(root,context={}){
 const{params={},fps=30}=context,{cards=["SOURCE","LOCK","INSTANCE"],activeIndex=1,spread=.68}=params,{element,gsap}=createFidelityHost(root,context,meta,"cst",CSS),stage=htmlNode("div","cst-stage"),nodes=[];stage.setAttribute("data-layout-allow-overlap","");cards.forEach((label,i)=>{const card=htmlNode("article","cst-card");card.setAttribute("data-layout-allow-overlap","");card.dataset.active=String(i===Number(activeIndex));card.append(htmlNode("div","cst-no",`LAYER / 0${i+1}`),htmlNode("div","cst-label",label));stage.append(card);nodes.push(card)});element.append(stage);const state={progress:0};function render(){nodes.forEach((n,i)=>{const d=i-Number(activeIndex);n.style.transform=`translate3d(${d*Number(spread)*44*state.progress}px,${-d*Number(spread)*24*state.progress}px,${-Math.abs(d)*70}px) rotateZ(${d*3.5*state.progress}deg)`;n.style.zIndex=String(10-Math.abs(d))})}const segments=standardTimelines(gsap,element,nodes,state,render);return{segments,seek(f){segments.enter.time(Math.max(0,f)/fps)},destroy(){destroyFidelity(element,segments)}}}
